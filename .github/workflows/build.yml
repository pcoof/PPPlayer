name: Build & Release
# ========== Workflow 说明 ==========
# 目标：自动构建 Windows 单文件 exe（PyInstaller --onefile --noconsole），并发布 GitHub Release。
# 1. calc-version：版本号 YYYYMMDD.N 自增（检索同日最大 Tag 序号 +1）。
# 2. build-package：用 calc-version 算出的版本写入 version.txt 并随 exe 嵌入（不在源码写死版本），
#    构建单文件 exe + 便携 zip，上传 artifact。
# 3. build-package：仅在 windows-latest 上用 uv + PyInstaller 产出单文件 exe，
#    打包为便携 zip，上传 artifact（不再构建 Linux/macOS、InnoSetup 安装包、wheel/sdist）。
# 4. release：生成 CHANGELOG、幂等创建 Git Tag、发布 GitHub Release 并上传产物。
# 并发锁防止多 push 并发造成 tag/release 冲突；版本在构建期写入 version.txt 嵌入 exe，无需回写源码。
# ==========================================
on:
  workflow_dispatch:
  push:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  GIT_USER_NAME: github-actions[bot]
  GIT_USER_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
  ARCHIVE_ROOT: ./archive
  RELEASE_ASSET_ROOT: ./release-assets

jobs:
  calc-version:
    runs-on: ubuntu-latest
    outputs:
      full_version: ${{ steps.gen_version.outputs.full_version }}
      date_prefix: ${{ steps.gen_version.outputs.date_prefix }}
      serial_num: ${{ steps.gen_version.outputs.serial_num }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-tags: true
          fetch-depth: 0

      - name: Generate date prefix YYYYMMDD
        id: date_calc
        run: |
          set -euo pipefail
          echo "DATE_PREFIX=$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: Count same date tag serial number
        id: gen_version
        run: |
          set -euo pipefail
          DATE_PREFIX=${{ env.DATE_PREFIX }}
          TAGS=$(git tag --list "v${DATE_PREFIX}.*")
          MAX_SERIAL=0
          for tag in $TAGS; do
            SERIAL=$(echo "$tag" | cut -d'.' -f2)
            if [[ "$SERIAL" -gt "$MAX_SERIAL" ]]; then
              MAX_SERIAL=$SERIAL
            fi
          done
          NEW_SERIAL=$((MAX_SERIAL + 1))
          FULL_VERSION="${DATE_PREFIX}.${NEW_SERIAL}"
          echo "date_prefix=${DATE_PREFIX}" >> $GITHUB_OUTPUT
          echo "serial_num=${NEW_SERIAL}" >> $GITHUB_OUTPUT
          echo "full_version=${FULL_VERSION}" >> $GITHUB_OUTPUT
          echo "Calculated Version: v${FULL_VERSION}"

  # 版本不再写死源码（已移除 update-version-file）：build-package 在构建期把版本写入
  # version.txt 并随 exe 嵌入，运行时由 get_app_version() 读取。

  build-package:
    needs: [calc-version]
    runs-on: windows-latest
    env:
      FULL_VER: ${{ needs.calc-version.outputs.full_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Python + uv
        if: hashFiles('pyproject.toml') != ''
        uses: astral-sh/setup-uv@v3
        with:
          version: latest
          python-version: "3.13"

      - name: uv sync resolve dependencies
        if: hashFiles('pyproject.toml') != ''
        run: uv sync --frozen

      - name: Prepare archive directory
        run: mkdir -p ${{ env.ARCHIVE_ROOT }}

      # 构建期把版本写入 version.txt 并随 exe 嵌入：运行时由 get_app_version() 读取，
      # 确保「发布 exe 内置版本 == Git Tag」，否则更新器会拿旧基准比对而误报「有更新」。
      - name: Write version into version.txt (build-time)
        shell: pwsh
        run: |
          $ver = "${{ env.FULL_VER }}"
          Set-Content -NoNewline -Path version.txt -Value $ver
          Write-Host "Embedded version.txt = $ver"

      # ================= Windows 单文件 exe =================
      - name: Windows PyInstaller single-file portable exe
        shell: pwsh
        run: |
          uv run pyinstaller `
            --onefile --noconsole --icon=logo.ico --name=ppplayer `
            --add-data "static;static" `
            --add-data "version.txt;." `
            --hidden-import clr --hidden-import pythonnet `
            --hidden-import webview.platforms.edgechromium --hidden-import webview.platforms.winforms `
            --hidden-import src --hidden-import src.server --hidden-import src.config_store `
            --hidden-import src.parser.sniffer --hidden-import src.parser.m3u8_filter `
            --hidden-import src.cms --hidden-import src.cms.detector --hidden-import src.cms.base `
            --hidden-import src.cms.apple_cms --hidden-import src.cms.feifei_cms `
            --hidden-import src.cms.haiyang_cms --hidden-import src.cms.endpoint `
            --collect-all pythonnet `
            main.py --noconfirm --clean
          if (-not (Test-Path dist/ppplayer.exe)) { throw "PyInstaller single-file build failed" }
          Copy-Item dist/ppplayer.exe ${{ env.ARCHIVE_ROOT }}/
          Compress-Archive -Force `
            -Path dist/ppplayer.exe `
            -DestinationPath ${{ env.ARCHIVE_ROOT }}/ppplayer-portable.zip

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-win-${{ env.FULL_VER }}
          path: ${{ env.ARCHIVE_ROOT }}/
          retention-days: 30

  release:
    needs: [calc-version, build-package]
    runs-on: ubuntu-latest
    env:
      FULL_VER: ${{ needs.calc-version.outputs.full_version }}
      CHANGELOG_FILE: ./generated_changelog.md
    steps:
      - name: Checkout full repo
        uses: actions/checkout@v5
        with:
          ref: main
          fetch-tags: true
          fetch-depth: 0

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ${{ env.RELEASE_ASSET_ROOT }}

      - name: Setup Git identity & auth
        run: |
          set -euo pipefail
          git config --global user.name "${{ env.GIT_USER_NAME }}"
          git config --global user.email "${{ env.GIT_USER_EMAIL }}"
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"

      - name: Create & push Git version tag (idempotent)
        run: |
          set -euo pipefail
          TAG="v${{ env.FULL_VER }}"
          if git ls-remote --tags --exit-code origin "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "Remote tag $TAG exists, clean-up old tag"
            git push origin ":refs/tags/$TAG" || true
          fi
          # 注意：git tag -l 无论是否匹配都返回 0，不能用作存在性判断；
          # 用 rev-parse -q --verify 检查 refs/tags/$TAG，不存在时退出码非 0，才跳过删除
          if git rev-parse -q --verify "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "Local tag $TAG exists, deleting it"
            git tag -d "$TAG"
          fi
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "$TAG"

      - name: Auto generate changelog file
        id: gen_changelog
        run: |
          set -euo pipefail
          echo "## Release v${{ env.FULL_VER }}" > ${{ env.CHANGELOG_FILE }}
          echo "Build Time: $(date -Iseconds)" >> ${{ env.CHANGELOG_FILE }}
          echo "" >> ${{ env.CHANGELOG_FILE }}
          echo "### Commit Changes" >> ${{ env.CHANGELOG_FILE }}
          PREV=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -n "$PREV" ]; then
            RANGE="${PREV}..HEAD"
          else
            RANGE="HEAD"
          fi
          git log --pretty=format:"- %s (%h)" $RANGE >> ${{ env.CHANGELOG_FILE }}
          echo "" >> ${{ env.CHANGELOG_FILE }}
          cat ${{ env.CHANGELOG_FILE }}

      - name: Delete existing release for idempotent rerun
        run: |
          set -euo pipefail
          TAG="v${{ env.FULL_VER }}"
          REL_ID=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/$TAG" | jq -r '.id // empty')
          if [ -n "$REL_ID" ]; then
            curl -s -X DELETE -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/releases/$REL_ID"
            echo "Old release deleted: $REL_ID"
          fi

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.FULL_VER }}
          name: Release v${{ env.FULL_VER }}
          body_path: ${{ env.CHANGELOG_FILE }}
          files: ${{ env.RELEASE_ASSET_ROOT }}/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
