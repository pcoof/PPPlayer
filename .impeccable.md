# 设计上下文 — TSPlayer 桌面端

## Design Context

### Users
个人影音爱好者，在 Windows 桌面端（pywebview / WinForms + Chromium）长时间浏览、搜索、收藏与播放影视。
核心场景：翻看封面墙 → 搜片 → 收藏/历史回顾 → 弹出独立窗口竖屏连播。重沉浸感、易扫读、低认知负担。

### Brand Personality
安静、克制、有书卷气（文艺）。像一本私人影评杂志 + 极简画廊，而不是炫技的科技播放器。
三个词：**雅致 · 克制 · 温润**。情绪目标：安心、专注、从容。

### Aesthetic Direction
- 主题名：**「纸上观影 / Paper & Reel」**
- 暖纸色背景 + 思源宋体标题（文艺气质）+ 单一暖金强调色（克制使用）+ 发丝级分隔线替代重阴影 + 大量留白。
- 深/浅双主题都精致，跟随 `cfg.theme`（auto/light/dark，class 切换 `dark`）。
- 反参考：纯黑科技风、霓虹紫蓝渐变、圆角+发光毛玻璃滥用、AI 默认紫(#5b47ff)配色。
- 参考气质：Infuse 的克制、纸书排版的呼吸感、美术馆展签的精致。

### Design Principles
1. 留白即内容——用间距与发丝线建立层级，少用卡片套卡片与重阴影。
2. 衬线标题建立文艺气质，正文用清晰无衬线，字号用流体 clamp()。
3. 单一暖金强调，仅在主操作/激活态出现；危险操作用克制的朱砂红。
4. 动效缓出（cubic-bezier(0.22,1,0.36,1)），禁止回弹；尊重 prefers-reduced-motion。
5. 双主题用同一套 token 驱动，保证深浅一致的高级感。

### 技术约束
- 复用现有 JS 接口（Alpine `app()`、player.js、api.js），不改变行为；仅改视觉层。
- 保留所有 `x-*` 绑定、`id`、`x-ref`、JS 依赖的类名（含 player.html 缺失的 `.tiktok-right-panel-2`/`.btn-close-right-panel-2`）。
- 通过 index.html 内联 `tailwind.config`（darkMode:'class' + 暖色板）修复此前未生效的 primary 调色板与媒体查询式暗色。
- 字体走 Google Fonts（Noto Serif SC / Noto Sans SC），带系统衬线/无衬线回退，离线不致崩。
