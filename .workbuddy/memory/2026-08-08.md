# 2026-08-08 工作日志

## 瀑布流加载动画替换
- 在 `static/css/style.css` 新增 `.loading-14` 转圈动画（用户指定的 #ddd 灰环 + #ff3d00 橙红环，含 `position:relative` 让 `:after` 绝对定位对齐）。
- `static/index.html` 瀑布流「加载更多」区：未加载时显示「加载更多」按钮，加载中（`loading`）显示 `.loading-14` 转圈；替换原「加载中…」文字。仅 waterfall 模式生效，page 模式不受影响。
- 注：原顶部全屏加载仍用 `.ts-spinner`（未动），仅按用户要求改了瀑布流局部。
